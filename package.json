{
  "name": "africa-dream-safaris",
  "version": "1.0.0",
  "description": "Custom WordPress theme for Africa Dream Safaris",
  "author": "Duo Studio",
  "scripts": {
    "sass": "sass --watch styles/style.scss:style.css --style compressed",
    "sass:build": "sass styles/style.scss:style.css --style compressed",
    "js:watch": "nodemon --watch scripts --ext js --ignore scripts/*.min.js --exec \"npm run js:build\"",
    "js:build": "terser scripts/global.js --compress --mangle --output scripts/global.min.js",
    "dev": "concurrently \"npm run sass\" \"npm run js:watch\" --names \"sass,js\" --prefix-colors \"blue,green\"",
    "build": "npm run sass:build && npm run js:build"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "nodemon": "^3.1.10",
    "sass": "^1.69.5",
    "terser": "^5.43.1"
  },
  "dependencies": {
    "lenis": "^1.3.8"
  }
}
